#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n; //Size of array
    cout << "Enter size of array: ";
    cin >> n;
    int arr[n]; // Make an array of size n
    cout << "Enter array elements: ";
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    return 0;
}